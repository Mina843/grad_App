plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.gg"
    compileSdk = 34
    ndkVersion = "25.1.8937393" // تأكد من تحديد إصدار NDK المناسب

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId "com.example.gg"
        minSdk = 23
        targetSdk = 34 // استبدال flutter.targetSdkVersion بقيمة ثابتة
        versionCode = 1 // تحديد قيمة رقمية ثابتة بدلاً من flutter.versionCode
        versionName = "1.0.0" // تحديد قيمة نصية ثابتة بدلاً من flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
